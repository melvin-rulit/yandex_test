# 🚀 Запуск проекта (Laravel + Vue + Docker)

Этот проект использует **Laravel** (backend), **Vue 3 + Vite** (frontend) и **Docker** (для окружения).  
Инструкция ниже позволит быстро поднять проект на любой машине.

---

## 📦 Требования

Перед началом убедитесь, что установлены:

- Docker + Docker Compose
---

## 📥 1. Клонирование репозитория

```bash
git clone https://github.com/your/project.git
cd project
```

## 🐳 Запуск проекта через Docker

```bash
docker-compose build
docker-compose up -d
```

📚 Установка зависимостей

```bash
composer install
```
📚 Выполнение миграций в контейнере:
```bash
 php artisan migrate
```

🖥️ Запуск фронтенда

```bash
команда запускается автоматически в контейнере
```

Вход в приложение
```bash
login: user@mail.com
password: qwerty123
```



