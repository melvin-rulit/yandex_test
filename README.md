# 🚀 Запуск проекта (Laravel + Vue + Docker)

Этот проект использует **Laravel** (backend), **Vue 3 + Vite** (frontend) и **Docker** (для окружения).  
Инструкция ниже позволит быстро поднять проект на любой машине.

---

## 📦 Требования
Перед началом убедитесь, что установлены:
- Docker + Docker Compose
---

## 📥 1. Клонирование репозитория
```bash
git clone https://github.com/melvin-rulit/yandex_test.git
cd project
```
📚 Установка переменных окружения:
```bash
По умолчанию .env.example содержит пустые или нейтральные значения.
Переименуйте .env.example в .env
Данные для поключения к базе данных уже прописаны
```

## 🐳 Сборка и запуск проекта через Docker
```bash
docker-compose build
docker-compose up -d
```

📚 Зайдите в PHP-контейнер командой (docker-compose exec php bash) из корня проекта и запустите команды:
```bash
composer install
php artisan key:generate
php artisan migrate
```

Вход в приложение
```bash
login: user@mail.com
password: qwerty123
```



